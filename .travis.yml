language: python
python:
  - "3.8"
install:
  - pip install -U pip
  - pip install -U pytest
  - pip install codecov

script:
  - coverage run -m unittest tests/test_fibonacci.py
  - coverage run -m unittest tests/test_two_sum.py

after_success:
  - codecov